#pragma once
#include <string>

void msg(std::string, int = 0);
void pcontinue();

void strlower(std::string&);
void wininit();
std::string replacesymbol(std::string, char, char);
int numEdit(int min, int max);